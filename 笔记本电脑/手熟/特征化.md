df.columns = df.columns.str.strip()

keyword = ['RTX 50', 'GDDR 7']

def check(row):
	text = str(row.get('显卡' ,0) + row.get('显存' , 0))
	text = text.upper()

	for kw in keyword:
		if kw.upper in text:
			return 1
	return 0

df['显卡类型'] = df.apply(check(). axis = 1)

SELECT *
	CASE
		WHEN '' LIKE '%%'  THEN 1
		WHEN '' LIKE '%%' THEN 1
		ELSE 0
	END AS ''
FROM TABLE